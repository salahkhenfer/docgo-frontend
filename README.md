# DocGo Frontend

## Environment

Copy `.env.example` to `.env`.

- `VITE_API_URL`: backend base URL (scheme + domain + optional path, no trailing slash)
    - `http://localhost:3000`
    - `https://api.docgo.com`
    - `https://docgo.com/api`

## Run locally

- `npm install`
- `npm run dev`
